# v3 F16 vs v4 F16 (2026-09-15)

Same 352-row stratified draw from `privacy_v4_test.jsonl` (20/bucket) plus 12 hand cases.

| | v3 prod (Chinese prompt, span only) | v4 (short prompt, ctx from key) |
|---|---|---|
| SECRET recall | 1.00 | 0.77 overall; **1.00 on keyed secrets** |
| precision | 0.31 | **1.00** |
| FPR | 0.79 | **0.00** |
| hostname / model / hard_fp / uuid / git FP | high (often 1.0) | **0** |
| span_only_secret recall | 1.00 | 0.00 if `c=` empty; **1.00 if `c=bearer token`** |

v4 train==serve: `Secret? k={key} v={span} c={ctx} ->`

Empty ctx always SAFE. Production Layer 1 now:

- assignment (`password=`): `c=env file`
- keyless residual tokenish: `c=bearer token`
- hostname-like keys: `c=example docs`

Production llama-privacy: `/opt/privacy-gateway/v4/qwen2.5-0.5b-privacy-v4-f16.gguf` (step-220 LoRA merge).

Readiness probe (2026-09-18): **FPR only** — `ok = negatives_kept == n_neg`. Weak passwords are **not** Layer 0; if Layer 1 says SAFE they leak. Layer 0 only covers structured tokens (`sk-`, JWT, PEM, …). Weights: https://huggingface.co/amwangfan/privacy-gateway-v4-qwen2.5-0.5b
